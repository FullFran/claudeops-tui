# Skill Registry — ClaudeOps-TUI

Auto-generated by sdd-init (2026-04-09). Lists user/project skills relevant to this Go + Bubbletea project.

## Compact Rules (auto-resolved for sub-agent injection)

### go-testing (auto-load when writing/touching Go test files)
- Use table-driven tests with `t.Run(name, ...)` subtests.
- Test files live next to source: `foo.go` → `foo_test.go`, package `foo` (or `foo_test` for black-box).
- Use `t.Helper()` in helper functions; `t.Cleanup()` for teardown.
- Prefer the standard library `testing` over assertion frameworks. Use `testify/require` only when comparing complex structs.
- Bubbletea TUIs: test models with `tea.NewProgram(model, tea.WithInput(...), tea.WithOutput(...))` driven by scripted msgs; use `teatest` (charmbracelet/x/exp/teatest) for snapshot-style assertions when justified.
- Run with `go test -race ./...` in CI; the race detector must be clean.
- Strict TDD Mode (enabled): write a failing test BEFORE implementation, see it fail, then make it pass.

### commit (when creating commits)
- Conventional commits only: `feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `chore:`.
- NEVER add `Co-Authored-By` or AI attribution. (User global rule.)
- Atomic commits: one logical change per commit.

### claude-md-management (when editing CLAUDE.md)
- Keep CLAUDE.md actionable and concise; no marketing copy.

## Project Conventions

- Source: `/home/franblakia/.claude/CLAUDE.md` (user global)
  - Strict TDD Mode: enabled
  - No AI attribution in commits
  - Senior Architect tone, push back on weak reasoning
  - Concepts > code; verify before agreeing

## User Skills Index (relevant subset)

| Skill | Trigger context |
|-------|-----------------|
| go-testing | writing Go tests, Bubbletea TUI tests |
| commit | creating git commits |
| pr / branch-pr | creating pull requests |
| simplify | reviewing changed code for quality |
| skill-creator | creating new agent skills |
